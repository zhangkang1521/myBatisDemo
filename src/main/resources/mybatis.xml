<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!-- 配置文件对应 org.apache.ibatis.session.Configuration -->

    <settings>
        <!-- 开启二级缓存 -->
        <setting name="cacheEnabled" value="true"/>
        <!--
            一级缓存，默认开启，为 SESSION 级别,还可以设置为 STATEMENT
            Mybatis的一级缓存最大范围是SqlSession内部，有多个SqlSession或者分布式的环境下，
            有操作数据库写的话，会引起脏数据，建议是把一级缓存的默认级别设定为Statement，即不使用一级缓存。
        -->
        <setting name="localCacheScope" value="STATEMENT"/>
    </settings>

    <environments default="development">
        <environment id="development">
            <!-- 事务管理类型，JDBC表示直接使用JDBC的提交和回滚设置，依赖于数据源得到的连接来管理事务 -->
            <transactionManager type="JDBC"/>
            <!-- 数据库连接池POOLED表示使用数据库连接池 -->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/zk"/>
                <property name="username" value="root"/>
                <property name="password" value="123456"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <!-- SQL代码和映射信息配置文件 -->
        <mapper resource="mappers/UserMapper.xml"></mapper>
        <mapper resource="mappers/ClassesMapper.xml"></mapper>
    </mappers>

</configuration>